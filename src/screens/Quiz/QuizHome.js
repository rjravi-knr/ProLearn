import React from 'react';
import {View, Text} from 'react-native';
import UiContainer from '../../components/UiContainer';

const QuizHome = () => {
  return <UiContainer gradiant></UiContainer>;
};

export default QuizHome;
